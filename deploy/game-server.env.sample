# 游戏 WebSocket 服务环境变量。这里只配置统一身份公钥，绝不能放主站私钥或 JWT 密钥。
PORT=21246
GAME_AUTH_PUBLIC_KEY=replace-with-jwks-x
GAME_AUTH_ISSUERS=https://haruyuki.cn

# 既有匿名统计配置；数据目录必须位于不可变发布目录之外。
STATS_DATA_DIR=/root/haruhi-game-data/statistics
STATS_HASH_SALT=replace-with-stable-random-salt
